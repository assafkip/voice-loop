[project]
name = "voice-loop"
version = "0.1.0"
description = "A voice system that learns from the drafts you rejected, not just the ones you published."
requires-python = ">=3.9"
dependencies = []
license = {text = "MIT"}

[project.scripts]
voiceloop = "voiceloop.cli:main"

[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"
